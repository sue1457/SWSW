< BFS, DFS > 
참고 자료 [https://itholic.github.io/python-bfs-dfs/
           http://ejklike.github.io/2018/01/05/bfs-and-dfs.html]
           
* BFS - Queue *
def bfs(graph, start_node):
       visit = list()
       queue = list()
  
       queue.append(start_node)
  
       while queue:
           node = queue.pop(0)
           if node not in visit:
              visit.append(node)
              queue.extend(graph[node])
 
      return visit

* DFS - Stack *
def dfs(graph, start_node):
       visit = list()
       stack = list()
  
       stack.append(start_node)
  
       while stack:
           node = stack.pop()
           if node not in visit:
              visit.append(node)
              stack.extend(graph[node])
 
      return visit

** DFS의 예시 문제 -  **
